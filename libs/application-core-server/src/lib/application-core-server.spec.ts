import { applicationCoreServer } from './application-core-server'

describe('applicationCoreServer', () => {
  it('should work', () => {
    expect(applicationCoreServer()).toEqual('application-core-server')
  })
})
