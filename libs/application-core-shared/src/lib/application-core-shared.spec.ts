import { applicationCoreShared } from './application-core-shared'

describe('applicationCoreShared', () => {
  it('should work', () => {
    expect(applicationCoreShared()).toEqual('application-core-shared')
  })
})
