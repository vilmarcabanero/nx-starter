import { applicationCoreWeb } from './application-core-web'

describe('applicationCoreWeb', () => {
  it('should work', () => {
    expect(applicationCoreWeb()).toEqual('application-core-web')
  })
})
